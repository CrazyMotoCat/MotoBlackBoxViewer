## Структура решения

- `MotoBlackBoxViewer.App` — WPF UI, встроенная карта, окно, ViewModel
- `MotoBlackBoxViewer.Core` — модели, CSV-парсер, анализ телеметрии
- `MotoBlackBoxViewer.Tests` — реальные unit-тесты для парсинга и статистики

# MotoBlackBoxViewer

Стартовый каркас Windows-приложения на **C# + WPF** для просмотра логов ESP32 с мотоцикла.

## Что уже есть
- загрузка CSV с разделителем `;`
- поддержка русских заголовков
- попытка чтения UTF-8, затем CP1251
- таблица точек
- простые графики скорости и угла наклона
- встроенная карта маршрута прямо в окне приложения через WebView2
- подсветка выбранной точки одновременно в таблице, на графиках и на карте
- экспорт маршрута в HTML-карту и открытие во внешнем браузере

## Формат CSV
Ожидаются колонки:
- `широта`
- `долгота`
- `скорость`
- `ускорение по Z`
- `ускорение по X`
- `ускорение по Y`
- `Угол наклона`

Регистр не критичен.

## Как открыть в Visual Studio
1. Откройте `MotoBlackBoxViewer.sln`.
2. Дождитесь восстановления NuGet-пакетов.
3. Запустите проект `MotoBlackBoxViewer.App`.

Целевая платформа: `net8.0-windows`.

## NuGet-пакеты
В проект уже добавлены:
- `Microsoft.Web.WebView2`
- `System.Text.Encoding.CodePages`

## Как работает карта
- Встроенная карта внутри окна использует `WebView2`.
- HTML-шаблон карты лежит в `Assets/Map/index.html`.
- Маршрут передается в JS из WPF и рисуется как polyline.
- Выбор строки в таблице подсвечивает ту же точку на графиках и на карте.
- Для подложки карты используется OpenStreetMap, поэтому для тайлов нужен интернет.

## Что можно делать дальше
- добавить ползунок воспроизведения маршрута
- добавить фильтры по диапазону точек
- показывать ускорения X/Y/Z на отдельных графиках
- экспортировать скрин карты и графиков
- добавить режим офлайн-карты


## Тесты
- проверка парсинга CSV с русскими и английскими заголовками
- проверка чисел с `,` и `.`
- проверка чтения CP1251 и UTF-8
- проверка расчета накопленной дистанции
- проверка агрегированной статистики

Запуск:
```bash
dotnet test MotoBlackBoxViewer.sln
```
